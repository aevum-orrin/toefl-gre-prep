# vocab-srs — spaced-repetition vocabulary trainer

A shared TOEFL/GRE flashcard web page driven by `prep_core.SRS` (the SM-2 algorithm, the same family
Anki started with). Scheduling only — no network cost; pronunciation uses the browser's built-in
speech synthesis (free, zero storage).

```bash
source ../../env.sh
uvicorn app:app --reload --port 8003   # http://localhost:8003
```

## How to use
- Switch **TOEFL / GRE / scenes** decks at the top (wordlists come from `toefl/vocab/` and `gre/vocab/`).
- See the word → **Enter** = "I know it cold", never shown again; or **Space** to reveal → grade:
  - **Again (1)** didn't recall → comes back later **today**, but only after ≥15 other cards
    (spacing-effect guard — a lapsed word never pops right back)
  - **Hard (2)** / **Good (3)** → recalled; the interval grows per SM-2
- Keyboard: `Space` reveal · `1/2/3` grade · `Enter` know-it · `←` undo · `→` redo.

## Pronunciation & phonetics
- Every card shows **US and UK IPA** (merged from open-dict-data/ipa-dict by `scripts/add_ipa.py`)
  with **4 speaker buttons**: US ♂ / US ♀ / UK ♂ / UK ♀ — all via the browser's Web Speech API.
- Each new card (and each undo) **auto-plays** once with the US male voice; toggle in the 🔊⚙ menu.
- Voices are picked per OS heuristically; override any of the 4 slots in 🔊⚙ (saved in localStorage).
- Each part-of-speech row also has an IPA + a US-male speaker button.

## On the reveal page
- **🌱 词根词缀 · 词源** — collapsed block above the primary definition (roots/affixes glossed in
  Chinese, meaning derivation, source language / loanword note). Generated by
  `scripts/enrich_etym.py` (free LLM, resumable cache on scratch); words without a genuinely
  helpful analysis show no block.
- **🔎 词汇检索** — collapsed in-deck substring search ("sub" → subsequent, submarine…). Clicking a
  hit opens that word as a full study page (grading it counts toward today); **⬅ 返回检索前**
  returns to the word you were on (a real back-stack, unlike ← undo which reverts grades).
- **📝 Personal Notes** — collapsed markdown notes box (Chinese/English/anything; paste GPT answers
  as-is). Autosaves to `$PREP_DATA_DIR/notes/vocab_notes.json`, keyed by term and shared across
  decks, so the note is there whenever you meet the word again. Rendered with vendored
  marked.js + DOMPurify (`static/vendor/`).

## Data
- Wordlist (content): deck JSON under each exam's `vocab/` folder (`term, senses, ipa_us, ipa_uk,
  etymology, tier, …`); built by `scripts/build_vocab.py`, enriched by `scripts/enrich_vocab.py` /
  `enrich_etym.py` / `add_ipa.py`.
- Review state (scheduling): `$PREP_DATA_DIR/srs/<deck>.json`, saved automatically on each grade.
- Personal notes: `$PREP_DATA_DIR/notes/vocab_notes.json`.
- Adding words: append to the wordlist JSON; new terms are merged in on startup and existing review
  progress is untouched.

> SM-2 is plenty for a personal sprint. To minimize review load later you can swap the scheduler for
> FSRS (Anki's current default) without changing the rest of the app — see the research notes under
> `docs/`.
